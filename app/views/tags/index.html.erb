<h1 class="title" style = "color:blue">Post</h2>

<% @posts.each do |post| %>
	<div class="post_wrapper">
		<h2 class="title"><%= link_to post.title, post %></h2>
		<p class="date"><%= post.created_at.strftime("%B %d, %Y") %></p>
	</div>
		Tags: <%= raw post.tags.map(&:name).map { |t| link_to t, tag_path(t) }.join(', ') %>

<% end %>
<h1 class="title" style = "color:blue">Article</h2>

<% @articles.each do |article| %>
	<div class="post_wrapper">
		<h2 class="title"><%= link_to article.title, article %></h2>
		<p class="date"><%= article.created_at.strftime("%B %d, %Y") %></p>
	</div>
		Tags: <%= raw article.tags.map(&:name).map { |t| link_to t, tag_path(t) }.join(', ') %>

<% end %>

